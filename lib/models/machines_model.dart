import 'package:flutter/material.dart';

class ListMachinesPageModel extends ChangeNotifier {
  // Initialize any state variables here if needed in the future

  ListMachinesPageModel();

  void initState(BuildContext context) {
    // Initialize any resources or listeners here
  }

  @override
  void dispose() {
    // Clean up resources like controllers, subscriptions, etc.
    super.dispose();
  }
}